name: Build DOCX from Markdown

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - "docs/*.md"
      - ".github/workflows/doc_pipeline.yml"
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-docx:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Ensure build directory exists
        run: |
          mkdir -p build
          touch build/.gitkeep

      - name: Convert Markdown to DOCX
        # Fixed the volume mapping and added --user root to solve mapping errors
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/data \
            -w /data \
            --user root \
            pandoc/core:latest \
            docs/ai_mastery_eval.md -o build/ai_mastery_eval.docx

      - name: Commit updated DOCX
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Add the .gitkeep and the docx
          git add build/
          
          if git diff --cached --quiet; then
            echo "No changes detected in build artifacts. Skipping commit."
            exit 0
          fi
          
          git commit -m "chore: auto-generate DOCX and maintain build directory [skip ci]"
          git push
